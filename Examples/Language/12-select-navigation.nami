select details {
    desc ["title": "Details"]

    case "close" => return
    case "quit" => exit "closed from details"
}

select menu {
    desc ["title": "Menu"]

    case "details" => goto details
    case "quit" => exit "closed"
}
